{% extends 'mail/base.html.twig' %}

{% block unsubscribe_link '' %}

{% block content %}
    <p>
        {{ 'mails.hello_named'|trans({ '%name%': actor.firstName }) }}
    </p>
    <p>
        {{ 'mail.actor.registration_complete.body'|trans }}
    </p>
    <p>
        {{ 'mail.common.click_here_to_sign_in'|trans({ '%url%': url('app_login') })|raw }}
    </p>
{% endblock %}
