{% extends 'mail/base.html.twig' %}

{% block body %}
    {{ 'common.hello'|trans({ '%actor%': actor.firstName }) }}
    <br>
    {{ 'mail.actor.reset_password_request.body'|trans({ '%url%': url('app_actor_reset_password_reset', { uuid: token }) })|raw }}
{% endblock %}
