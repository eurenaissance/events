{% extends 'mail/base.html.twig' %}

{% block body %}
    {{ 'common.welcome'|trans({ '%actor%': actor.firstName }) }}
    <br>
    {{ 'mail.actor.registration_confirmation.body'|trans({ '%url%': url('app_actor_registration_confirm', { uuid: token }) })|raw }}
{% endblock %}
