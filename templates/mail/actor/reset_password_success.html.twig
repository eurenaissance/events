{% extends 'mail/base.html.twig' %}

{% block body %}
    {{ 'common.hello'|trans({ '%actor%': actor.firstName }) }}
    <br>
    {{ 'mail.actor.reset_password_success.body'|trans }}
    <br>
    {{ 'mail.common.click_here_to_sign_in'|trans({ '%url%': url('app_login') })|raw }}
{% endblock %}
