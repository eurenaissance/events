{% set animator = group.animator %}

{% extends 'mail/base.html.twig' %}

{% block body %}
    {{ 'common.hello_actor'|trans({ '%actor%': animator.firstName }) }}
    <br>
    {{ 'mail.group.created.body'|trans({ '%group%': group.name })|raw }}
{% endblock %}
