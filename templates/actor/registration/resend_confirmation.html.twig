{% extends 'base.html.twig' %}

{% block title 'actor.registration.resend_confirmation'|trans %}

{% block body %}
    {{ form_start(form)}}
        {{ form_row(form.emailAddress) }}

        <button type="submit">
            {{ 'actor.registration.resend_confirmation'|trans }}
        </button>
    {{ form_end(form) }}

    <a href="{{ path('app_login') }}">
        {{ 'security.back_to_login'|trans }}
    </a>
{% endblock %}
