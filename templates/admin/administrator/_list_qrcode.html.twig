{% if object.googleAuthenticatorSecret %}
    <a href="{{ path('app_admin_qr_code', { id: object.id }) }}"
       title="QR code Google Authenticator"
       class="btn btn-sm btn-default"
       target="_blank">
        <i class="fa fa-qrcode" aria-hidden="true"></i>
        QR code Google Authenticator
    </a>
{% endif %}
